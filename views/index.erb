<div class="container">
    <h1>OCPUB <span class="version">v0.12</span></h1>
    <p>The Octo CPU Benchmark measures the time it takes to calculate the number <strong>π with <%= $precision %> decimals</strong>. The calculation is <strong>repeated <%= $repetition %> times</strong> and an average calculation duration is determined.
    <hr/>
    <p>
        <strong>Execution Date:</strong> <%= @execution_date %><br/>
        <strong>Average Calculation Duration:</strong> <%= @average_duration.round(6) %>s
    </p>
    <table>
        <tr>
            <th>Round</th>
            <th>Duration</th>
        </tr>
        <% $durations.each_with_index do |duration, index| %>
            <tr>
                <td><%= index+1 %></td>
                <td><%= duration.round(6) %>s</td>
            </tr>
        <% end %>
    </table>
    <hr/>
    <p>
        Source code on <a href="https://github.com/dipree/ocpub">GitHub</a>
    </p>
</div>